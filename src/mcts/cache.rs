
pub struct TranspositionTable {
    array: 
    capacity: u32,
    mask: u32,
    probe_limit: u8,
}

pub struct TranspositionEntry {
    array:
    capacity: u32,
    mask: u32,
    probe_limit: u8,
}

